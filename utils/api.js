const host = 'https://article.hgsp.cn:10449',
  openidUrl = `${host}/openid`,
  letterUrl = `${host}/letter`,
  addLetterUrl = `${host}/addletter`


module.exports = {
  host,
  openidUrl,
  letterUrl,
  addLetterUrl
};